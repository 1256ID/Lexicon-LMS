@page "/teacher/courses/{CourseId:int}/modules/edit/{ModuleId:int}"

@inject NavigationManager Navigation
@inject HttpClient HttpClient
@inject IJSRuntime JSRuntime

@using LMS.Blazor.Client.Components.Forms
@using Microsoft.AspNetCore.Authorization

@attribute [Authorize(Roles = "Teacher")]

<PageTitle>Edit Module</PageTitle>


<div class="content">
    <!-- Breadcrumb with course name -->
    <nav aria-label="breadcrumb" class="mb-5">
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a href="/teacher/courses" class="text-decoration-none">
                    <i class="bi bi-book me-1"></i>Courses
                </a>
            </li>
            <li class="breadcrumb-item">
                <a href="/teacher/courses/@CourseId" class="text-decoration-none">
                    @courseName
                </a>
            </li>
            <li class="breadcrumb-item">
                <a href="/teacher/courses/@CourseId/modules" class="text-decoration-none">
                    Modules <!-- @moduleName -->
                </a>
            </li>
            <li class="breadcrumb-item active">Edit Module</li>
        </ol>
    </nav>


    @* <!-- WITH API - Uncomment the loading state:
    @if (isLoading)
    {
            <div class="text-center py-5">
                <div class="spinner-border text-success" role="status">
                    <span class="visually-hidden">Loading...</span>
                </div>
                <p class="text-muted mt-3">Loading module...</p>
            </div>
    }
    else if (module == null)
    {
            <div class="alert alert-danger" role="alert">
                <i class="bi bi-exclamation-triangle me-2"></i>Module not found
            </div>
    }
    else
    {
        --> *@

        <div class="row">
                <div class="col-12">
                        <!--EDIT MODE: Pre-filled with Wireframing module data -->
                        <ModuleForm HeaderTitle="Edit Module"
                                              SubmitButtonText="Update Module"
                                              ShowDeleteButton="true"
                                              InitialModuleName="Wireframing and Prototyping"
                                              InitialDescription="Learn to create wireframes and interactive prototypes using industry-standard tools. This module covers low-fidelity sketching, digital wireframing, and prototyping techniques for web and mobile applications."
                                              InitialStartDate="@DateTime.Today.AddDays(-5)"
                                              InitialEndDate="@DateTime.Today.AddDays(21).AddDays(-5)"
                                              OnCancel="@HandleCancel"
                                              OnSubmit="@HandleSubmit"
                                              OnDelete="@HandleDelete" />

                        <!-- With Module model:
                        <ModuleForm Module="@module"
                                              OnValidSubmit="@HandleSubmit"
                                              OnCancel="@HandleCancel"
                                              OnDelete="@HandleDelete"
                                              HeaderTitle="Edit Module"
                                              HeaderIcon="bi-pencil"
                                              SubmitButtonText="Update Module"
                                              SubmitIcon="bi-check-lg"
                                              ShowDeleteButton="true"
                                              ErrorMessage="@errorMessage"
                                              SuccessMessage="@successMessage"
                                              IsSubmitting="@isSubmitting" />
                        -->
                </div>
        </div>

        <!--WITH API - Close the if:
        }
    -->
</div>

@code {
    [Parameter] public int CourseId { get; set; }
    [Parameter] public int ModuleId { get; set; }

    private string courseName = "Introduction to UI/UX Design"; // Mock data

    // ===== VARIABLES FOR WHEN YOU HAVE THE API =====
    // Uncomment later:
    // private Module? module;
    // private bool isLoading = true;
    // private bool isSubmitting = false;
    // private string errorMessage = string.Empty;
    // private string successMessage = string.Empty;

    protected override async Task OnInitializedAsync()
    {
        await LoadCourseName();
    }

    private async Task LoadCourseName()
    {
        // TEMPORARY - Mock data
        await Task.Delay(100);
        // Real implementation will load course name from API
    }

    // ===== LOADING METHOD FOR API =====
    // Uncomment and call in OnInitializedAsync later:
    /*
    private async Task LoadModule()
    {
        try
        {
            isLoading = true;
            errorMessage = string.Empty;

            // API call to get module and course info
            var moduleResponse = await HttpClient.GetAsync($"api/modules/{ModuleId}");
            var courseResponse = await HttpClient.GetAsync($"api/courses/{CourseId}");

            if (moduleResponse.IsSuccessStatusCode && courseResponse.IsSuccessStatusCode)
            {
                module = await moduleResponse.Content.ReadFromJsonAsync<Module>();
                var course = await courseResponse.Content.ReadFromJsonAsync<Course>();
                courseName = course.Name;
            }
            else
            {
                errorMessage = "Failed to load module or course information";
            }
        }
        catch (HttpRequestException ex)
        {
            errorMessage = $"Network error: {ex.Message}";
        }
        catch (Exception ex)
        {
            errorMessage = $"Unexpected error: {ex.Message}";
        }
        finally
        {
            isLoading = false;
        }
    }
    */

    private void HandleSubmit()
    {
        Navigation.NavigateTo($"/teacher/courses/{CourseId}/modules");
    }

    private void HandleCancel()
    {
        Navigation.NavigateTo($"/teacher/courses/{CourseId}/modules");
    }

    private async Task HandleDelete()
    {
        var confirmed = await JSRuntime.InvokeAsync<bool>("confirm",
            "Are you sure you want to delete this module?");

        if (confirmed)
        {
            Navigation.NavigateTo($"/teacher/courses/{CourseId}/modules");
        }
    }
}
