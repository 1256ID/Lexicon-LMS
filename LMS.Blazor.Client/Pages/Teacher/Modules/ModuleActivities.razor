@using LMS.Blazor.Client.Services
@rendermode InteractiveWebAssembly
@inject NavigationManager Nav
@inject NavigationStateService NavState
@using LMS.Blazor.Client.Models
@using LMS.Blazor.Client.Components.Cards

<div class="row">
    @foreach (var a in ActivitiesList)
    {
        <div class="col-lg-4 col-md-6 col-sm-12 mb-4">
            <ActivityCard Activity="a"
                          OnOpen="OpenActivity"
                          OnEdit="EditActivity" />
        </div>
    }
</div>

@code {
    [Parameter] public int CourseId { get; set; }
    [Parameter] public int ModuleId { get; set; }

    private List<ActivityVM> ActivitiesList = new()
    {
        new ActivityVM  {Id=1, Title="Intro Video", Description="Video explaining course intro",  StartDate= DateTime.Parse("2025-08-14"), EndDate= DateTime.Parse("2025-08-15")},
        new ActivityVM  {Id=2, Title="Quiz 1", Description="First quiz for the module",  StartDate= DateTime.Parse("2025-08-16"), EndDate= DateTime.Parse("2025-08-17")},
        new ActivityVM  {Id=3, Title="Assignment", Description="Hands-on assignment", StartDate= DateTime.Parse("2025-08-18"), EndDate= DateTime.Parse("2025-08-19")}
    };

    private void OpenActivity(ActivityVM a)
    {
        var moduleName = NavState.CurrentModuleName ?? $"Module {ModuleId}";
        NavState.SetActivity(ModuleId, moduleName, a.Id, a.Title);
        Nav.NavigateTo($"/teacher/course/{CourseId}/module/{ModuleId}/activity/{a.Id}");
    }

    private void EditActivity(int id)
    {
        Nav.NavigateTo($"/teacher/course/{CourseId}/module/{ModuleId}/activity/edit/{id}");
    }
}





