@using LMS.Blazor.Components.Layout
@inject NavigationManager Navigation

 @page "/test"

<div class="container mt-4">

    <h1 class="mb-4">Test Page</h1>
    <p>This is a test page to demonstrate the layout and components.</p>
  
   
    <div class="row">
        <div >
            <CourseCard />
        </div>
        <div ">
            <ModuleCard/>
        </div>
        <div >
            <ActivityCard/>
        </div>
        <div>
            <ModuleNavigationButtons/>
        </div>
        <div>
            <CourseNavigationButtons/>
        </div>


    </div>
</div>
            

@*
    <!-- Courses -->
    <h2 class="mb-3">Courses</h2>
    <div class="row">
        @foreach (var course in Courses)
        {
            <div class="col-md-3 mb-3">
                <CourseCard Course="course" OnClick="() => Navigation.NavigateTo($"/modules/{course.Id}")" />
            </div>
        }
    </div>

    <!-- Modules -->
    <h2 class="mb-3 mt-5">Modules</h2>
    <div class="row">
        @foreach (var module in Modules)
        {
            <div class="col-md-3 mb-3">
                <ModuleCard Module="module" OnClick="() => Navigation.NavigateTo($"/activities/{module.Id}")" />
            </div>
        }
    </div>

    <!-- Activities -->
    <h2 class="mb-3 mt-5">Activities</h2>
    <div class="row">
        @foreach (var activity in Activities)
        {
            <div class="col-md-3 mb-3">
                <ActivityCard Activity="activity" OnClick="() => OpenActivity(activity.Id)" />
            </div>
        }
    </div>

</div>

@code {
    // private List<Course> Courses = new List<Course>
    // {
    //     new Course { Id=1, Name=".NET 2025", Description="Learn .NET and C#", StartDate=DateTime.Today },
    //     new Course { Id=2, Name="Javascript Basics", Description="Intro to JS", StartDate=DateTime.Today.AddDays(7) }
    // };

    // private List<Module> Modules = new List<Module>
    // {
    //     new Module { Id=1, Name="Databasdesign", Description="Learn DB structure", StartDate=DateTime.Today, EndDate=DateTime.Today.AddDays(7) },
    //     new Module { Id=2, Name="Javascript", Description="JS basics", StartDate=DateTime.Today.AddDays(8), EndDate=DateTime.Today.AddDays(14) }
    // };

    // private List<Activity> Activities = new List<Activity>
    // {
    //     new Activity { Id=1, Name="E-learning", Type="E-learning", Description="Online lesson", StartTime=DateTime.Today, EndTime=DateTime.Today.AddHours(1) },
    //     new Activity { Id=2, Name="Lecture", Type="Föreläsning", Description="Instructor session", StartTime=DateTime.Today.AddHours(2), EndTime=DateTime.Today.AddHours(3) }
    // };

    // private void OpenActivity(int activityId)
    // {
    //     Console.WriteLine($"Open activity {activityId}");
    // }
}
 *@